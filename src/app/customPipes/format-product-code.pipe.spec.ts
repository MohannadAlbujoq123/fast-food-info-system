import { FormatProductCodePipe } from './format-product-code.pipe';

describe('FormatProductCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatProductCodePipe();
    expect(pipe).toBeTruthy();
  });
});
